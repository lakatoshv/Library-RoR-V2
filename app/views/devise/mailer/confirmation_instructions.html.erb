<h2 class="col-md-6 col-md-offset-3 text-center back "><%= t ".h2" %> <%= @email %>!</h2>
<div  class="col-md-4 col-md-offset-4 back text-center show_h2_content">
	<p><%= t ".p" %></p>
	<% confirm = t ".confirm" %>
	<p><%= link_to confirm, confirmation_url(@resource, confirmation_token: @token) %></p>
</div>
